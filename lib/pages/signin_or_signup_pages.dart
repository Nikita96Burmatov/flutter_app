import 'package:flutter/material.dart';


class SigninOrSignupPages extends StatelessWidget {
  const SigninOrSignupPages({Key? key}) : super(key:key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(
        child: Column(
          children: [
            Image.asset(name)
          ],
        ),
      ),
    );

  }
}